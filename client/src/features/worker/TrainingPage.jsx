import { useState } from 'react'
import './training.css'

const sequence = ['Isolate vehicle power and wear PPE', 'Inspect battery terminals and harness insulation', 'Measure battery voltage and alternator output', 'Confirm the repair and return the vehicle to service']
export default function TrainingPage() { const [index, setIndex] = useState(0); const xr = typeof navigator !== 'undefined' && !!navigator.xr; return <section className="training-page"><p className="eyebrow">VR TRAINING SANDBOX</p><h1>Battery diagnostics simulation</h1><p>Practice the repair sequence safely before working on a live fleet vehicle.</p><div className="training-scene"><div className="training-vehicle"><i/><b>TRUCK #245</b><span>Battery bay diagnostic simulation</span></div><div><small>TRAINING STEP {index + 1} OF {sequence.length}</small><h2>{sequence[index]}</h2><button className="primary" onClick={() => setIndex((index + 1) % sequence.length)}>Next training step</button>{xr && <button className="secondary">Enter VR</button>}{!xr && <p className="muted">WebXR hardware not detected. This interactive 3D-style fallback remains fully usable.</p>}</div></div></section> }
